{
    "exp_name": "example",
    "num_epochs": 10,
    "num_iter_per_epoch": 10,
    "learning_rate": 0.001, #! check
    "batch_size": 16,
    "state_size": [784],
    "max_to_keep":5
  }

  # look up hyperparameters that really matter
  # momentum, etc. 